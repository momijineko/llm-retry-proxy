# 号池 CSV 配置模板
# 格式: key,url,provider,label,models,paths（首行为表头，不区分大小写）
#   key      - API key（必填）
#   url      - 上游地址，留空=用 UPSTREAM_URL（可选）
#   provider - 供应商标签，留空=用 PROVIDER（可选）
#   label    - key 标签/备注，用于日志显示和分组统计（可选，留空=用 key 前8位）
#   models   - 仅供匹配模型使用，多个 glob 用分号分隔（可选）
#   paths    - 仅供匹配请求路径使用，多个 glob 用分号分隔（可选）
# 行序即优先级：上面的=低倍率(便宜)，下面的=高倍率(贵)
# # 开头的行为注释，不会被加载
#
# 配置后在 .env 中设置: KEY_POOL_FILE=key_pool.csv
# 然后把此文件复制为 key_pool.csv 开始使用

key,url,provider,label,models,paths
sk-your-cheap-group1,,,低倍率组,,
sk-your-normal-group2,,,中倍率组,,
sk-your-premium-group3,,,高倍率组,,
# 生图专用分组示例：模型或路径命中时只使用这些行
# sk-image-group,https://example.com,example,生图组,gpt-image-*;imagen-*,images/*;v1/images/*
# 多上游示例（取消注释后生效）:
# sk-other-upstream-key,https://other.com,other,备用站,,
